# An i3 Config for a 2 Monitor Setup
This i3 Config setup is made for 2 Monitors.

Make sure to change the monitor name variables ```($fm & $sm)``` defined in ```workspaces.conf``` to the ones you have.
You can check them using the ```xrandr``` command
